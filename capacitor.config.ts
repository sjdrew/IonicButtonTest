import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'IonicButtonTest',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
